# Images

| file | used for |
|---|---|
| `logo.svg` | header logo + favicon (gold keycap with a "j" and belt-motion streaks) |

Still-open drop-in points:

| file | would be used for |
|---|---|
| `avatars/<username>.png` | profile avatar — currently an initial in a gold circle (see the `IMAGE PLACEHOLDER` comment in `index.html`) |
| `bg.webp` | optional page background texture (wire into `body` background in `css/style.css` — an animated CSS aurora currently fills that role) |
